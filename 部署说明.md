# 产业带智能看板 · 线上版部署说明

## 一、最简部署（任意 Windows / Linux / Mac 服务器）

### 前置要求
- Node.js 18 或更高版本（https://nodejs.org/）

### Windows 一键启动
1. 解压本 zip 到任意目录（例如 `D:\industrial-belt-dashboard\`）
2. 双击 `启动.bat`
3. 浏览器打开 http://localhost:8080
4. 默认账号 `admin` / `admin123`（首次登录请改密）

### Linux / Mac 启动
```bash
unzip 产业带智能看板_线上版.zip
cd industrial-belt-dashboard
npm install --omit=dev   # 首次需要
node server.js
```

## 二、生产部署（推荐）

### pm2 守护
```bash
npm install -g pm2
pm2 start server.js --name industrial-belt
pm2 save
pm2 startup           # Linux 开机自启
```

### 端口和防火墙
- 默认端口 8080，可改 `server.js` 顶部的 `PORT` 常量
- 云服务器需要在安全组放行对应端口
- 推荐前置 Nginx 反代 + HTTPS 证书

### Nginx 反代示例
```nginx
server {
    listen 80;
    server_name your-domain.com;
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 三、目录结构

```
industrial-belt-dashboard/
├── server.js                # 主服务（Express）
├── package.json             # 依赖声明
├── 启动.bat                 # Windows 一键启动
├── 部署说明.md              # 本文档
├── data/
│   ├── industrial-belts.json  # 211 家供应商数据
│   └── users.json             # 账号数据（admin/admin123）
├── public/                  # 前端资源（HTML/CSS/JS/中国地图GeoJSON）
├── uploads/                 # 用户上传文件存放目录
└── node_modules/            # 已预装依赖（开箱即用）
```

## 四、账号管理

- 主账号 `admin` 登录后右上角 ⚙ 设置面板：
  - 添加子账号
  - 修改任意账号密码
  - 上传/管理共享文件
- 子账号仅能改自己密码

## 五、数据扩充

- 新增供应商：编辑 `data/industrial-belts.json`，按现有 JSON 结构追加
- 或运行 `node expand-data.js` 复用扩充脚本（按需修改）
- 改动数据无需重启服务（前端刷新即生效）

## 六、常见问题

| 问题 | 排查 |
|------|------|
| 8080 端口被占用 | 改 `server.js` 中 `PORT`，或 `netstat -ano \| findstr 8080` 杀掉占用进程 |
| 浏览器打不开 | 防火墙是否放行 8080 端口；查看启动窗口有无报错 |
| 忘记密码 | 删除 `data/users.json`，重启后自动重建默认 admin/admin123 |
| 中国地图不显示 | 确认 `public/data/china.json` 存在（582KB） |

## 七、版本

- 本包打包日期：2026-04-30
- 包含数据：211 家真实供应商，覆盖 20+ 核心产业带
- 离线单文件版（不含登录/上传）：`产业带智能看板_完整版.html`
